<div class="container">
  <div class = "comapany_summary">
    <h2><%= @company.name %></h2>
    <%= image_tag @company.image.url %>
  </div>
  <div class = "">
    <%= link_to "レビューを書く", new_company_review_path(@company) %>
  </div>
  <div class = "">
    <% @company.reviews.each do |review| %>
      <%= review.comment %>
      <%= review.user.name %>
    <% end %>
  </div>
</div>