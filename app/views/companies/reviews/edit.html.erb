<div class="container">
  <%= form_for(@review) do |f| %>
    <h3>
      編集する
    </h3>
    
    <div class="field">
      <%= f.label :"レビュー" %><br />
      <%= f.text_area :comment, name: :comment %>
    </div>
      <%= f.submit "更新する", class: "btn btn-primary" %>
  <% end %>
</div>