<div class="container">
  <%= form_for [@company, @review] do |f| %>
    <h3>
      投稿する
    </h3>
    <div class="field">
      <%= f.label :"レビュー" %><br />
      <%= f.text_area :comment %>
    </div>
      <%= f.submit "投稿", class: "btn btn-primary" %>
  <% end %>
  </div>